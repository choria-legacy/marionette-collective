The Marionette Collective
=========================

The Marionette Collective aka. mcollective is a framework to build server orchestration or parallel job execution systems.

For full information, wikis, ticketing and downloads please see http://marionette-collective.org/

New in Kontera's Version?
=========================
- Removed all 3rd party vendors, using gems instead.
- MCollective now can be packed into gem (rake gem).
- Installation of MCollective trough rubygems (gem install mcollective).
- New installer script available inside the gem, use it to install
-- MCollective Server
-- MCollective Client
-- MCollective Common Plugins

`[root$] mcollective_install --help
Usage: mcollective_install [options] client,server,common,full (default full)

Options:
    -r, --ruby_path PATH             default is ruby
    -c, --config_path PATH           default is /etc/mcollective
    -p, --plugins_path PATH          default is /usr/libexec/mcollective
    -b, --bin_path PATH              default is /usr/sbin
    -C, --custom_config_file PATH    e.g /etc/conf/my_server.cfg
    -F, --custom_facts_yaml PATH     e.g /etc/conf/my_facts.yaml
    -f, --force                      replace files, default is false
`